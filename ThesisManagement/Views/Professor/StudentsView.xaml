<UserControl x:Class="ThesisManagement.Views.Professor.StudentsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ThesisManagement.Views.Professor"
             xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
             xmlns:customControls="clr-namespace:ThesisManagement.CustomControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="white">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="50"/>
            <RowDefinition />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="120"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0"
                        Orientation="Horizontal"
                        VerticalAlignment="Top"
                        FlowDirection="LeftToRight">
                <fa:IconImage Icon="Search"
                              Margin="0 4 5 0"
                              Height="17" 
                              Width="20"/>
                <TextBox Text=""   
                         Height="24"
                         Width="200"
                         BorderThickness="0,0,0,2"
                         VerticalContentAlignment="Center">
                </TextBox>

            </StackPanel>

        </Grid>

        <Border Grid.Row="1" Padding="10 0">
            <StackPanel Orientation="Horizontal">
                <RadioButton Style="{StaticResource tabBtn}" IsChecked="True"
                     Command="{Binding ShowWaitingStudentsView}">
                    <TextBlock Text="Đang chờ" Style="{StaticResource tabText}" />
                </RadioButton>

                <RadioButton Style="{StaticResource tabBtn}"
                     Command="{Binding ShowProfessorsView}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Đã duyệt" Style="{StaticResource tabText}" />
                    </StackPanel>
                </RadioButton>
                
                <RadioButton Style="{StaticResource tabBtn}"
                     Command="{Binding ShowProfessorsView}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Đã từ chối" Style="{StaticResource tabText}" />
                    </StackPanel>
                </RadioButton>
            </StackPanel>
        </Border>

        <Border Grid.Row="2" Padding="0 10 0 0">
            <local:WaitingStudents />
        </Border>      
    </Grid>
</UserControl>
