<UserControl x:Class="ThesisManagement.Views.Student.RegisterTopicView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ThesisManagement.Views.Student"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:customControls="clr-namespace:ThesisManagement.CustomControls"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000"
             Background="White">
    <UserControl.Resources>
        <Style x:Key="Button1Style" TargetType="Button">
            <Setter Property="Background" Value="Green"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border Background="{TemplateBinding Background}" BorderBrush="Green" BorderThickness="1" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="White"/>
                    <Setter Property="Foreground" Value="Green"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        

        <Style x:Key="Button2Style" TargetType="Button">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="Green"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border Background="{TemplateBinding Background}" BorderBrush="Green" BorderThickness="1" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="Green"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="100"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="80"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <Grid Grid.Column="0">
                <Image Source="/Resources/Images/UTE.png" Height="70" Width="70" Margin="10 0 0 0" />
            </Grid>
            <Border Grid.Column="1">
                <TextBlock 
                        Text="Đề tài : Ứng dụng bản đồ số xây dựng website tìm kiếm và cho thuê bất động sản khu vực Hà Nội " 
                        Style="{StaticResource title}"
                        TextWrapping="Wrap"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"/>
            </Border>
        </Grid>
        <Grid Grid.Row="1">
            <ScrollViewer Grid.Row="2" Grid.ColumnSpan="2"  Background="White">

                <StackPanel >
                    <StackPanel Orientation="Vertical" Margin="20,10,30,30">
                        <Label Content="MÔ TẢ " FontSize="20" FontWeight="Bold" Foreground="Green" MaxWidth="450" HorizontalAlignment="Left" />
                        <ListView ItemsSource="{Binding YourItemList}">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="" Margin="5"/>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </StackPanel>
                    
                    <StackPanel Orientation="Vertical" Margin="20,10,30,30">
                        <Label Content="Yêu cầu " FontSize="20" FontWeight="Bold" Foreground="Green" MaxWidth="450" HorizontalAlignment="Left" />
                        <ListView ItemsSource="{Binding YourItemList}">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="" Margin="5"/>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </StackPanel>

                    <StackPanel Orientation="Vertical" Margin="20,10,30,30">
                        <Label Content="Số lượng thành viên" FontSize="20" FontWeight="Bold" Foreground="Green" MaxWidth="450" HorizontalAlignment="Left" />
                        <ListView ItemsSource="{Binding YourItemList}">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="" Margin="5"/>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </StackPanel>
                    
                    <StackPanel Orientation="Vertical" Margin="20,10,30,0">
                        <Label Content="Thời gian" FontSize="20" FontWeight="Bold" Foreground="Green" MaxWidth="450" HorizontalAlignment="Left" />
                        <ListView ItemsSource="{Binding YourItemList}">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="" Margin="5"/>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </StackPanel>

                </StackPanel>

            </ScrollViewer>
        </Grid>
        <Grid Grid.Row="2">
            <customControls:ControlButton DefaultColor="{StaticResource primaryBrush}"
                                          HoverColor="{StaticResource secondaryBrush}"
                                          Width="110" Margin="0 0 0 0">
                    <customControls:ControlButton.Content>
                        <TextBlock Text="Đăng Kí" Style="{StaticResource btnContent}"/>
                    </customControls:ControlButton.Content>
                </customControls:ControlButton>
            
        </Grid>
    </Grid>
</UserControl>
